<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003" ToolsVersion="4.0"  >

  <!-- test runtime targets -->
  <Import Project="$(TestFrameworkReferencesRoot)Trace.targets" />
  <Import Project="$(TestFrameworkReferencesRoot)Test.targets" />
  <Import Project="$(TestFrameworkReferencesRoot)Tasks.targets" />
  
</Project>
